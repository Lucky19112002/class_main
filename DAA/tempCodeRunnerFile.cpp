for (int i = 0; i < sizeofarray1; i++)   // Loop for bubbleshortarray 1000 to 1
  // {
  //   bubbleshortarray1[i] = j;
  //   j--;
  // }
  // auto start = high_resolution_clock::now();
  // ios_base::sync_with_stdio(false);
  // bubbleshort(bubbleshortarray1);
  // auto stop = high_resolution_clock::now();
  // auto duration = duration_cast<microseconds>(stop - start);
  
  // cout<<"Time taken by function 1000 to 0 is : "<<fixed<<duration.count()<< " microseconds" << endl;
  // for (int i = 0; i < sizeofarray1; i++)   // Loop for bubbleshortarray
  // {
  //   bubbleshortarray2[i] = x;
  //   x++;
  // }
